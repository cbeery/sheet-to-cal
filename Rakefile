desc "REBUILD Stem Sheet from Steam Airtable"
task :rebuild_stems_from_steam do
	load 'airtable-to-sheet-rebuild.rb'
end

desc "UPDATE Stem Sheet from Steam Airtable"
task :update_stems_from_steam do
	load 'airtable-to-sheet-update.rb'
end
